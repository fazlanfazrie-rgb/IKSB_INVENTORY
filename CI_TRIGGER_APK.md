# TranX_Store APK CI Trigger

This file intentionally triggers GitHub Actions on the latest tranx-store-app commit so the repaired Flutter source is validated and the Android APK build can run.

Target: Analyze -> Test -> Android APK -> Windows Release.
